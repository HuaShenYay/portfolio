# Portfolio Studio

Standalone Sanity Studio for the portfolio website.

```bash
npm install
npm run dev
```

The Studio runs on `http://localhost:3333` by default and connects to project `mzz2m6c7`, dataset `production`.
